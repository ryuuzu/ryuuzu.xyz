import { ColorWheel } from "iconoir-react";

const Loading = () => {
    return (
        <div className="flex justify-center">
            <ColorWheel className="animate-spin text-5xl text-secondary" />
        </div>
    );
};

export default Loading;
